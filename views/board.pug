extends layout

block content

    include toolbar


    v-content.brown.lighten-5.animation-fade-in(style="padding-top: 50px")
        //v-container.pa-0(:fluid="$vuetify.breakpoint.xsOnly || $vuetify.breakpoint.smOnly")
        v-container.pa-0(fluid)

            v-flex(md12='')
                //v-card.pa-3.safe-area-inset(:class="$vuetify.breakpoint.xsOnly || $vuetify.breakpoint.smOnly  ? 'elevation-0 mb-4 pt-4' : 'elevation-10 my-4'")
                v-card.pa-3.safe-area-inset.elevation-0.mb-4

                    v-card-title.pb-0(style="margin-top: 20px;")
                        span.display-2(style="font-weight: 800;") 목록
                        v-spacer
                        v-menu(bottom, left)
                            v-btn(slot='activator', icon)
                                v-icon more_vert
                            v-list.dal-card
                                //v-list-tile(v-for="(toolbar, i) in toolbarItems", :key="i", :href="'#' + toolbar.href")
                                //    v-list-tile-title {{toolbar.title}}

                    v-card-text.py-0
                        v-divider.my-3

                    v-card-title.pa-0(v-if="items.length > 0")
                        //v-container(v-bind='{ [`grid-list-lg`]: true }', :class="$vuetify.breakpoint.xsOnly ? 'pa-2 pt-4' : '' ")
                        v-layout(v-bind='{ [`grid-list-lg`]: true }', :class="$vuetify.breakpoint.xsOnly ? 'pa-2 pt-2' : '' ")
                            v-layout(wrap)
                                //v-flex(xs12, sm6, md6, lg4 v-for="item in searchItems", :key="item.id", v-model='searchItems', v-if="isShowComplete || item.status !== 'COM'")
                                v-flex(xs12, sm6, md4, lg3 v-for="item in items", :key="item.id", v-model='items', :class="$vuetify.breakpoint.xsOnly ? '' : 'pa-2' ")
                                    include card_item

    include footer
    //include bottom_tab

    v-fab-transition
        v-btn(color='red', v-model='fab', dark, fab, fixed, bottom, right, onclick="window.location.href = '/board/regist'")
            v-icon edit
            //v-icon close

    script(type="text/javascript" src='/javascripts/board-config.js')
    script.
        const vue = init('!{user}');